<%= form_with(model: director) do |form| %>
  <% if director.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(director.errors.count, "error") %> prohibited this director from being saved:</h2>

      <ul>
        <% director.errors.each do |director| %>
          <li><%= director.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<form>
  <div class='form-group'>
    <%= form.label "Nome", style: "display: block" %>
    <%= form.text_field :name, class: "form-control"%>
  </div>
  
  <div class="mt-3 d-flex justify-content-end">
    <%= link_to "Cancelar", directors_path, class: "btn btn-white"%>
    <%= form.submit "Salvar", class: "btn btn-pink ms-2"%>
  </div>
</form>
<% end %>
